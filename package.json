{
  "private": true,
  "name": "miniprogram",
  "version": "0.1.0",
  "license": "GPL-3.0",
  "scripts": {
    "dev": "gulp dev",
    "build": "cross-env NODE_ENV=production gulp build",
    "test": "echo test",
    "prettier": "pretty-quick --staged"
  },
  "pre-commit": [
    "prettier"
  ],
  "devDependencies": {
    "cross-env": "^5.2.0",
    "gulp": "^4.0.2",
    "gulp-load-plugins": "^1.5.0",
    "pre-commit": "^1.2.2",
    "prettier": "^1.17.0",
    "pretty-quick": "^1.10.0"
  }
}
